# جدول التتبّع — قواعد المخزون من الدليل إلى المحرّك

مطلب مراجعة: عبارة «كل قاعدة عمل صارت قيدًا على المحرّك» لا تُقبل بلا جدول
تتبّع. هذا هو الجدول — **بصدق كامل**: ما نُفِّذ يُذكر بموضعه واختباره، وما هو
سلوك واجهة أو لم يُنفَّذ بعد يُعلَن كذلك ولا يُموَّه.

المصدر: `baytak-design-team-manual/inventory.html` (القواعد التشغيلية والحركات
والمبدأ الحاكم) + قرارات DECISIONS §8–§9 المشتقة منها.

الاختبارات المذكورة في `supabase/tests/` — أسماء التحققات حرفية.

## المبدأ الحاكم

| القاعدة (الدليل) | التنفيذ | الاختبار |
|---|---|---|
| «سجل الحركة هو مصدر الحقيقة — لا يُعدَّل الرصيد مباشرة» | لا عمود رصيد في أي جدول؛ الاشتقاق حصرًا عبر `core.movement_effects` × `private.roll_balance` و`api.roll_balances`؛ الدفتر غير قابل للتعديل (`stock_movements_immutable`) | `consume_and_release`: «دفتر حركة المخزون غير قابل للتعديل — حتى كـpostgres» + invariants بعد كل عملية |
| الحركات العشر المسماة في الدليل | `core.movement_type` (+`overconsumption` و`damage_reserved` المضافتان بقرارات موثقة، سياسة add-only) مع FK إلزامي إلى `movement_effects` | «كل قيمة enum لها صف في movement_effects» + «قيد FK … قائم» |

## القواعد التشغيلية الست

| # | القاعدة (الدليل) | التنفيذ | الاختبار |
|---|---|---|---|
| 1 | «الحجز داخل Transaction مع `SELECT … FOR UPDATE`» | `api.reserve_fabric`: قفل صف الرول ثم الفحص والكتابة في معاملة واحدة؛ ترتيب أقفال ملزم ينتهي بالمشروع | `reserve_fabric`: «التزامن: نجح واحد فقط» — اتصالان حقيقيان |
| 2 | «لا يمكن أن يصبح الرصيد المتاح سالبًا» | رفض `BD422` قبل أي كتابة في الدوال الست؛ الاشتقاق المُقصّ عند الصفر | سباقا الحجز والإرجاع + «الرصيد لم يصر سالبًا» + invariants |
| 3 | «لكل عملية `idempotency_key` لمنع التكرار» | `unique (org, idempotency_key)` + بصمة v3 كاملة المدخلات مطبَّعة، إعادة النتيجة قبل فحوص الحالة | «إعادة نفس المفتاح تعيد النتيجة» · «بمدخلات مختلفة يُرفض» · «تعمل رغم تغيّر إصدار المشروع» — في كل RPC |
| 4 | «اختلاف Dye Lot يظهر كتحذير واضح» | **غير مفروض على المحرّك — سلوك واجهة بالتصميم.** `dye_lot` مخزَّن ومعروض في views؛ التحذير مسؤولية شاشة الحجز عند ربط التطبيق | ❌ لا اختبار محرّك — بند مرحلة ربط الواجهة |
| 5 | «البقايا تُرجَع للرول أو تُحوَّل Mini Roll» | **جزئي:** الإرجاع للرول منفَّذ (`return_consumed_fabric` بسقف صارم وربط خماسي)؛ تحويل Mini Roll **RPC لم يُبنَ بعد** (`is_mini_roll` جاهز في المخطط، والمنطق حاليًا في مخزن التطبيق التجريبي فقط) | الإرجاع: مجموعة كاملة (سقف، تزامن، صلاحية admin) · Mini Roll: ❌ دين مسجَّل |
| 6 | «أي هدر يحتاج سببًا ومستخدمًا وتاريخًا» | `reason_code` FK إلزامي للحركات الاستثنائية (قيد + محفّز نطاق) و`created_by`/`created_at` `not null` | «بلا رمز سبب مرفوضة» ×3 · «غير معتمد لهذه العملية» · «المحفّز يرفض … حتى كـpostgres» |

## قواعد أُضيفت بالمراجعات (ليست في الدليل الأصلي)

| القاعدة | التنفيذ | الاختبار |
|---|---|---|
| الزيادة عن الحجز حدث مستقل لا يلوّث تصحيحات الجرد | `overconsumption` + `operation_group_id` خادمي التوليد | «حركتان منفصلتان» · «تحملان operation_group_id نفسه» |
| التلف ليس تحريرًا — لا في الدفتر ولا في الحالة ولا في الطوابع | `damage_reserved` مستقلة · `closed` محايدة · `released_at` للتحرير النقي و`finalized_at` لأي نهاية | اختبارات الأختام الأربعة + «تحرير كامل بعد تلف → closed» |
| الإرجاع مربوط بمصدر استهلاك بسقف | FK خماسي + `fabric_usage_link_shape` + سقف تحت قفل السجل | اختبارات NULL الخمسة + السقف + السباق |
| `expected_project_version` يُحسم تحت قفل المشروع | `FOR UPDATE` على المشروع آخر الأقفال في الدوال الخمس | «يُرفض تحت القفل رغم مرور الفحص المبكر» |
| invariant الحجز | قيد `reservation_balance_invariant` على المحرّك | «قيد invariant يرفض consumed+released+damaged > quantity» |

## الفجوات المفتوحة — بلا تمويه

*(حُدّثت 13.8.2026 بعد اكتمال خارطة الكتابة الحية - PRs ‏6-15)*

1. **تحذير Dye Lot** — واجهة؛ يُبنى مع شاشة الحجز.
2. **Mini Roll RPC** — غير مبني؛ التحويل محجوب بصدقٍ في الوضع الحي.
3. ~~ربط شاشات التطبيق بالدوال~~ — **أُغلقت**: كل الأفعال اليومية موصولة
   حيًا (زبائن، مشاريع، غرف وشبابيك، زيارات، عروض وخصومات، مخزون كامل،
   أوامر إنتاج، دفعات وشيكات ودفتر طاقم). ما لا RPC له يرفض برسالة صريحة
   بدل نجاحٍ محلي يتبخر: الحجز التلقائي، تسويات المخزون غير التلف،
   Mini Roll، المرفقات، الإعدادات والتسعيرة، إنشاء الموظفين، علامات
   القراءة على الإشعارات.
4. ~~التحقق المستقل عبر CI~~ — **أُغلقت**: ci.yml على main يشغّل بوابة
   TS وثمانية طقوم SQL (بينها crud/inventory_write/payments الجديدة).
5. **ترحيلات بانتظار التطبيق على خادم الإنتاج** (بالترتيب):
   `20260812120001` الأعمدة، `20260812120002` مرآة المحرك،
   `20260812150001` الاستلام والإتمام، `20260812190001` إشعارات العروض،
   `20260812210001` الزبائن والمشاريع، `20260812230001` الغرف والشبابيك،
   `20260813090001` الزيارات، `20260813110001` أوامر الإنتاج،
   `20260813140001` المال ودفتر الطاقم.
