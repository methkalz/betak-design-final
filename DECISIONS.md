# قرارات معمارية — بيتك ديزاين

قرارات مثبَّتة تحكم كل عمل لاحق. أي انحراف عنها يحتاج تحديث هذا الملف أولًا.

---

## 2026-08-02

### 1. مسار الكتابة — هجين محدَّد

**القراءة** عبر views آمنة حسب الدور. **الكتابة البسيطة** مباشرة على الجداول
المسموح بها مع RLS وقيود صارمة. **العمليات الحساسة أو متعددة الخطوات** عبر
RPC transactional حصرًا.

| كتابة مباشرة مع RLS | RPC حصرًا |
|---|---|
| إنشاء وتعديل الزبون | حجز القماش وتحريره |
| تعديل عنوان أو ملاحظة | الاستهلاك والإرجاع والتلف |
| إنشاء مشروع كمسودة | أي تعديل في رصيد المخزون |
| إضافة غرفة أو شباك | إنشاء واعتماد نسخة عرض سعر |
| إدخال قياسات | طلب واعتماد الخصم |
| إضافة موعد أو ملاحظة | تسجيل دفعة أو عكسها |
| أي بيانات غير مالية وغير مخزنية | قبول العرض وتفعيل المشروع |
| | إغلاق أو إلغاء المشروع |
| | أي عملية تمس عدة جداول وتجب كوحدة واحدة |

**قيد تقني اكتُشف عند التنفيذ:** PostgREST يعرض سكيما `api` وحدها، فـ`core`
غير موجودة في سطح الـAPI. لذا «الكتابة المباشرة على الجدول» تتحقق عبر **view
تمريري 1:1** بلا joins مع `security_invoker` — يجعله بوستجرس قابلًا للكتابة
تلقائيًا، فالتطبيق يكتب عليه وكأنه الجدول وتنطبق RLS والقيود كما هي. هذا ليس
Writable View بالمعنى المعقّد، بل ممر شفاف.

**اصطلاح التسمية:**
- `api.<entity>` — تمريري 1:1، **قابل للكتابة**
- `api.<entity>_details` — مثرى بـjoins، **قراءة فقط**
- `api.<entity>_summary` — تجميعي، قراءة فقط
- `api.<entity>_financials` — أعمدة التكلفة والربح، محكوم بشرط دور

قاعدة ثابتة: **view الخياط لا يحتوي التكلفة أو الربح أصلًا** — لا مخفية ولا مصفّرة.

### 2. المصادقة — بريد + كلمة سر

Supabase Auth بالبريد وكلمة السر. الأدمن ينشئ الحسابات ويوزّعها. بلا تكلفة
وبلا مزود خارجي. يمكن إضافة الهاتف لاحقًا دون كسر شيء.

`Profile.pin` في النموذج الأولي **محذوف نهائيًا** — رقم سري في جدول ليس مصادقة.

الجلسة تُحفظ في **SecureStore** (Keychain / Keystore) لا في AsyncStorage.

### 3. العمل دون إنترنت — للميدان فقط

القياس والتركيب والصور تعمل دون شبكة وتُزامَن لاحقًا عبر طابور.

المال والمخزون والخصومات **تتطلب اتصالًا ولا تُطبَّق تفاؤليًا أبدًا** — لا تُعتبر
مؤكدة قبل نجاح الـRPC. هذا نص الدليل حرفيًا.

كل عملية حساسة تحمل `client_operation_id` و `idempotency_key` و `lock_version`.

### 4. هوية التطبيق — `com.baytakdesign.app`

| | |
|---|---|
| iOS `bundleIdentifier` | `com.baytakdesign.app` |
| Android `package` | `com.baytakdesign.app` |
| الرابط العميق `scheme` | `baytakdesign` |

يحل محل `app.rork.72xauvl81ef7xdo88xgur` المولَّد. **يجب أن يُثبَّت قبل أول رفع
للمتجر** — بعده يستحيل تغييره عمليًا.

### 5. شاشة الخياط — قرار مقصود لا نتيجة عرضية

`api.tailor_project_details` **لا يحوي**: التكلفة، الربح، الخصم، اسم الزبون،
هاتفه، عنوانه. اختبار آلي (18b) يتحقق من غياب هذه الأعمدة من **تعريف** الview.

لكن الخياط يحتاج تمييز الرزم عمليًا، فالview يحمل:
`code` (مثل `BD-1041`) و `title` و `window_count` و `total_fabric_meters`
و `installation_date`.

`title` نص حر يكتبه الموظف، وقد يضع فيه اسم الزبون («بيت مثقال — كفر قاسم»).
هذا **مقبول ومقصود**: الاسم ليس سرًّا ماليًا، والخياط يحتاجه للتمييز. أما ما
يبقى محجوبًا فعلًا فهو الهاتف والعنوان والأرقام المالية.

إن أردنا خصوصية أعلى لاحقًا، الحل تقييد `title` أو إضافة
`project_display_name` منفصل — لا تغيير الview.

### 6. مالك دوال RPC — `baytak_rpc_owner`

دور `nologin` بـ`bypassrls`، يملك دوال `api` الحساسة بدل `postgres`.
`bypassrls` لازم لأن `FORCE ROW LEVEL SECURITY` يشمل المالك وكل سياساتنا
`to authenticated`. الحدّ الحقيقي هو **الصلاحيات الجدولية المسمّاة**: تجاوز
الصفوف لا يخلق صلاحية على جدول لم يُمنح عليه شيء.

قيود اكتُشفت عمليًا على هذا البناء (`supabase/postgres:15.8.1.085`):

| القيد | التفصيل |
|---|---|
| `GRANT role TO current_user` | **يُسقط الخادم** — خلل في supautils. استخدم الاسم الصريح. |
| PostgreSQL 15 | مُنشئ الدور لا يصير عضوًا تلقائيًا (تغيّر في 16)، فالمنح إلزامي قبل `ALTER … OWNER`. |
| `ALTER … OWNER TO` | تشترط `CREATE` على السكيما للمالك الجديد، لا `USAGE` وحدها. |
| سكيما `auth` | مملوكة لـ`supabase_admin`، و`postgres` يحمل `USAGE` بلا `GRANT OPTION` → لا يمكن تمريرها. الحل `private.current_uid()` بدل `auth.uid()`. |
| `SELECT … FOR UPDATE` | تتطلب صلاحية `UPDATE` لا `SELECT` فقط، حتى دون تعديل الصف. |

### 7. المخطط التصريحي — نموذج هجين

`supabase/schemas/` مصدر الحقيقة للبنية، و`supabase/migrations/` السجل
التاريخي غير القابل للتعديل. الاثنان معًا لا أحدهما.

**السبب ليس تنظيميًا:** توثيق Supabase يذكر صراحةً أن `db diff` لا يتتبّع
`security_invoker`، والـ39 view عندنا تعتمد عليه — بدونه تتجاوز RLS كليًا.
كذلك لا يتتبّع الملكية والمنح وصلاحيات الأعمدة والسكيمات والتعليقات و DML.
فتلك كلها تبقى في migrations يدوية.

**دوال قاعدة البيانات في `supabase/schemas/` لا في `supabase/functions/`** —
الأخير محجوز لـEdge Functions.

**الترتيب اعتمادي لا أبجدي.** اكتُشف عمليًا: `can_see_project` تسبق `role_in`
أبجديًا وتستدعيها، و`inventory_roll_balances` تعتمد على `roll_balances`،
وسياسات RLS تستدعي `has_role`. الترتيب مفروض في `config.toml` صراحةً —
**لا تستبدله بـ`./schemas/*.sql`**.

**التحقق:** `supabase/tests/schema_drift.py` يبني قاعدة مؤقتة من الملفات
ويقارن بصمة الكتالوج مع الحية. آخر نتيجة: 709 كائنًا متطابقًا، NO DRIFT.

### 8. محاسبة المخزون — المرجع المركزي وسياسة الـenum

**`core.movement_effects` هو المصدر الوحيد لأثر الحركات.** كل رصيد يُحسب
`sum(quantity_m × sign)` بالانضمام إليه — ممنوع تضمين مصفوفة أثر يدوية في أي
view أو RPC أو اختبار. (الاختبار نفسه وقع في هذا الفخ: نسخة يدوية قديمة فيه
أعطت نتائج خاطئة بعد إضافة النوع الجديد — فأُصلح ليشتق من الجدول.)

| الحركة | on_hand | reserved |
|---|---|---|
| receipt / return / adjustment_in / transfer_in | + | 0 |
| reservation | 0 | + |
| reservation_release | 0 | − |
| **consumption** | − | − |
| **overconsumption** | − | 0 |
| damage (من المتاح) / adjustment_out / transfer_out | − | 0 |

**سياسة enum:** قيم `movement_type` تُضاف فقط — لا يُعاد تسميتها ولا تُحذف
(حذف قيمة enum في PostgreSQL شبه مستحيل). لذا بقيت `consumption` باسمها
وأُضيفت `overconsumption` — الزيادة عن الحجز لم تعد `adjustment_out` فلا
تختلط بتصحيحات الجرد في تقارير الهدر.

**`operation_group_id`** يربط حركات الإجراء الواحد (استهلاك 30 + زيادة 5)،
ويُولَّد داخل الـRPC حصرًا — لا يُقبل من الجهاز.

**invariant الحجز** (يفرضه قيد على المحرّك، وسّعناه قبل بناء RPC التلف لا بعده):

```
quantity_m = consumed_m + released_m + damaged_reserved_m + remaining
```

والصيغ محصورة في مكانين: `private.reservation_remaining` و `private.roll_balance`.

**قواعد idempotency الملزمة** (بعد مراجعة كود كشفت فجوتين):
- البصمة القانونية تشمل **كل مدخل يؤثر فيما يُكتب** — الكمية والسبب
  والملاحظات، لا الكمية وحدها. نفس المفتاح ببصمة مختلفة → BD400.
- `p_expected_project_version` **ليس** جزءًا من البصمة: شرط تزامن لا هوية عملية.
- ترتيب ملزم في كل RPC: هوية وعضوية ودور ← بصمة ← فحص idempotency
  (إعادة النتيجة إن طابقت) ← **ثم** فحوص الحالة والإصدار للعمليات الجديدة
  فقط. إعادة إرسال طلبٍ نجح يجب أن تعيد نتيجته حتى لو تغيّر المشروع بعده.
- شكل البصمة v2 منذ 0027 — عمليات ما قبله لا يُعاد تشغيلها بمدخلات v2.

**دلالة `fabric_usage`:** الصف = **حدث استهلاك** لا ملخص حجز.
`planned_m` = الجزء المغطى بالحجز في هذا الحدث، فيصح لكل صف
`actual_m = planned_m + waste_m`، و`Σplanned ≤ quantity_m` للحجز.
(كانت «المتبقي قبل العملية» فيتضخم المجموع مع الاستهلاك الجزئي المتكرر.)

**قرار محسوم — تلف الكمية المحجوزة = قيمة enum مستقلة `damage_reserved`**
(أثر −/−)، لا حركتَي `damage` + `reservation_release`. الحجة الحاسمة من
مراجعة الطاقم: التلف ليس تحريرًا — استعمال `reservation_release` كان سيجعل
تقارير التحرير ترى كمية «حُرِّرت» وهي في الحقيقة تلفت. حدث تجاري واحد =
حركة محاسبية واحدة. التنفيذ (في فرع الإرجاع والتلف): ترحيل enum مستقل ←
صف `movement_effects` ← إضافتها إلى `reason_required_for_exceptions`
و`reservation_required` ← زيادة `damaged_reserved_m` ذريًا ← أنواع TS
والتقارير والاختبارات. `operation_group_id` غير لازم لحركة واحدة.

**عقد `return_consumed_fabric` الملزم (قبل بنائه):** الإرجاع الحر إلى رول
منفذ لتضخيم المخزون. كل إرجاع يرتبط بسجل استهلاك محدد (`fabric_usage_id`)
بسقف صارم: `الإرجاع الجديد ≤ actual_m − المرتجع سابقًا`، مع قفل سجل
الاستخدام أثناء الإرجاع، دعم الإرجاع الجزئي المتكرر، اختبار إرجاعين
متزامنين، منع الإرجاع إلى رول مختلف، حركة `return` (+1/0)، عدم تخفيض
`consumed_m` وعدم إعادة فتح الحجز — المرتجع يعود متاحًا ومن يحتاجه يحجز
من جديد. يتطلب ربطًا صريحًا بين حركة الإرجاع وسجل الاستهلاك (عمود
`usage_id` على دفتر الحركة أو ما يعادله — يُصمم في الفرع).

### 9. عقود الإرجاع والتلف — مثبَّتة قبل كتابة أي RPC

تنفيذ شروط الطاقم الخمسة (الترحيلات 0029–0031). **الدالتان لم تُكتبا بعد** —
هذا عقد البيانات والصلاحيات والحالة الذي ستُكتبان عليه.

**أ. آلة حالة الحجز** — دالة مركزية واحدة `private.reservation_status_for`:

| الشرط | الحالة |
|---|---|
| remaining > 0 و consumed > 0 | `partially_consumed` |
| remaining > 0 و consumed = 0 | `active` (ولو وُجد تلف/تحرير جزئي — الأرقام تحكي التفاصيل) |
| remaining = 0 و consumed = quantity | `consumed` (نقي) |
| remaining = 0 و released = quantity | `released` (نقي) |
| remaining = 0 غير ذلك | **`closed`** — مزيج أو أي تلف؛ الحالة لا تسمي التلف استهلاكًا ولا تحريرًا |

**ب. ربط الإرجاع — مفروض على المحرّك:** عمود `fabric_usage_id` على دفتر
الحركة، مع **FK خماسي** `(org, usage, roll, reservation, project)` إلى فهرس
فريد مطابق على `fabric_usage`. **الـRPC يستقبل `p_fabric_usage_id` ولا يقبل
`roll_id` من العميل** — كل السياق يُشتق من السجل.

⚠️ **الـFK وحده لا يكفي** (ثغرة أمسكتها المراجعة وأُكِّدت تجريبيًا ثم سُدَّت
في 0032): بـ`MATCH SIMPLE` أي NULL في أعمدة الـFK يعفي الصف من المطابقة
كليًا — فكان `return` برولٍ مختلف يمرّ إذا تُرك المشروع والحجز فارغين.
القيد المكمل `fabric_usage_link_shape`: كل `return` يحمل السياق الكامل
(usage + project + reservation)، وأي حركة غير `return` لا تحمل
`fabric_usage_id` أصلًا. (`MATCH FULL` مرفوض — كان سيكسر الحركات غير
المرتبطة التي تحمل مشروعًا أو حجزًا.)

**سبب الإرجاع:** `reason_code` **إلزامي على المحرّك** (`return` مضمومة إلى
`reason_required_for_exceptions`) و`notes` اختيارية — أسباب مرمّزة تجعل
تقارير الإرجاع قابلة للتجميع.

**ج. سقف الإرجاع:** `الإرجاع الجديد ≤ actual_m − Σ المرتجع سابقًا` محسوبًا
تحت قفل `FOR UPDATE` على سجل الاستخدام (الفهرس الجزئي
`stock_movements_usage_returns_idx` يخدم الجمع). الإرجاع لا يخفض
`consumed_m` ولا يعيد فتح الحجز — يعود متاحًا، ومن يحتاجه يحجز من جديد.

**د. مصفوفة الصلاحيات** (مقترحة للتصديق — القاعدة: الإرجاع عملية مخزون،
والتلف المحجوز يقع عند الخياط):

| العملية | admin | sales | الخياط المسند | field |
|---|---|---|---|---|
| `return_consumed_fabric` | ✅ | ❌ | ❌ (يسلّم فعليًا ولا يسجّل) | ❌ |
| `record_reserved_damage` (`damage_reserved`) | ✅ | ❌ | ✅* | ❌ |
| `record_stock_damage` (`damage` من المتاح) | ✅ | ❌ | ❌ | ❌ |

**المصفوفة نهائية بقرار صاحب المشروع (2026-08-03):** المبيعات لا يستلم
القماش المرتجع فعليًا → الإرجاع وتلف المخزون **للأدمن حصرًا**. توسيعها لاحقًا
يكون بدور مستقل (`warehouse`) لا بتوسيع sales.
\* شرط تنفيذ الخياط: التحقق من أنه المسند حاليًا **يُعاد بعد قفل**
المشروع/الحجز لا قبله — منفَّذ ومُختبَر (فكّ الإسناد ثم المحاولة → BD403).

**نموذج الأسباب (0033):** `reason_code` FK إلى `core.movement_reasons`
(قائمة معتمدة قابلة للتوسعة) + `notes` نص حر منفصل — **الدمج ممنوع**، فهو
يفسد تجميع التقارير. القيد `reason_code_required_for_exceptions` يفرض الرمز
على: damage · damage_reserved · adjustment_in/out · overconsumption · return.
العمود القديم `reason` أعيدت تسميته `notes` في `stock_movements` و
`fabric_usage` معًا.

**هـ. بصمات idempotency** (v3 — كل مدخل مؤثر، بلا `expected_project_version`):

```
consume_fabric         : op, user_id, reservation_id,  quantity_m, reason_code, notes
release_reservation    : op, user_id, reservation_id,  quantity_m, reason_code, notes
return_consumed_fabric : op, user_id, fabric_usage_id, quantity_m, reason_code, notes
record_reserved_damage : op, user_id, reservation_id,  quantity_m, reason_code, notes
record_stock_damage    : op, user_id, roll_id,         quantity_m, reason_code, notes
```

ترقية v3 آمنة تاريخيًا **بدليل**: استعلام الطاقم الحرفي على
`client_operations` أعاد صفر صفوف قبل الترحيل 0033 — لا عمليات v2 حقيقية.

**قاعدة التطبيع الملزمة (v3، مطبقة بأثر رجعي على consume/release في 0032):**
تُطبَّع المدخلات **قبل** حساب البصمة، و**القيمة المطبَّعة نفسها هي المخزنة**:
الكمية `round(…, 3)` · السبب والملاحظات `btrim` · `NULL` و`''` تمثيل واحد
(`''`). بدونها: مسافة زائدة = «عملية مختلفة»، أو تُخزن قيمة غير التي حُسبت
منها الهوية.

**ز. أختام الإنهاء (0036):** `released_at` = التحرير الكامل **النقي** فقط
(`status = released`)؛ `finalized_at` = دخول أي حالة نهائية
(consumed / released / closed)، يُختم مرة واحدة. التلف والإغلاق المختلط
**لا يمسّان** `released_at` — نفس قاعدة الدفتر تسري على الطوابع الزمنية.

**ح. ذرية فحص الإصدار (0036):** ترتيب الأقفال الملزم:
`fabric_roll ← fabric_reservation ← fabric_usage ← project` — والمشروع
**دائمًا الأخير**. فحص `expected_project_version` (وإسناد الخياط حيث يلزم)
الحاسم يجري **بعد** `FOR UPDATE` على المشروع؛ الفحص المبكر إخفاق سريع فقط.
مُثبَت باختبار سباق حقيقي: جلسة تمسك قفل المشروع وترفع الإصدار، والثانية
تمرّ من الفحص المبكر ثم تُرفض `BD409` تحت القفل.

**ط. نطاقات الأسباب (0036):** كل رمز له `applies_to movement_type[]` —
مفروض في الدوال (رسالة `BD400` واضحة) وبمحفّز `enforce_reason_scope` على
الدفتر (يصدّ المسارات مرتفعة الصلاحية). `water_damage` لتحرير حجز لم يعد
يمرّ. الرمز يخص الزيادة في consume — الاستهلاك المخطط يُخزَّن بلا رمز.

**و. الأثر المحاسبي** (صفوف `movement_effects` — `damage_reserved` مضافة):

```
return          +1 / 0    (يرفع المتاح)
damage          −1 / 0    (من المتاح)
damage_reserved −1 / −1   (المتاح ثابت — كالاستهلاك المخطط)
```

`operation_group_id` غير لازم للحركات المفردة الثلاث.

### 10. عقود طبقة العروض — معدَّلة بجولة المراجعة السابعة

فرع `feat/quotation-versioning`. **بنية العلاقات والحالات (بنود المراجعة
1، 2، 3، 5، 7) منفَّذة محركيًا** في ترحيلَي `130001/130002` بمجموعة اختبار
خاصة (22 فحصًا). **التصديق المشروط تحقق**: اعتراضات جولة التصديق الخمسة
أُغلقت نصًا في هذا القسم («مصادَق — ابدأ البناء» بشرطها)، وبُني عليها
**محرك التسعير والدوال الأربع** في ترحيلَي `150001/150002` بمجموعتَي
اختبار (32 فحص دورة حياة + 8 متجهات ذهبية من الجانبين SQL وTS).
المتبقي من الطبقة: **دوال الخصم (طلب/قرار)** ثم PR عبر البوابة الإلزامية
(Ruleset مفعّلة وموثّقة الإثبات).

**أ. الحالات بدقة — مستويان:**

النسخة (`quotation_versions.status`):
```
draft ──send──→ sent ──approve──→ approved   (نهائية)
  │               │──reject────→ rejected    (نهائية)
  │               │──نسخة أحدث أُرسلت──→ superseded (نهائية)
  │──نسخة أحدث أُنشئت──→ superseded          (نهائية)
```
- **الانقضاء مصدر حقيقة واحد (بند 3):** لا تثبيت عرضيًا. `effective_status`
  في `api.quotation_versions` يعرض `expired` متى كانت `sent` و`valid_until`
  مضى؛ وRPC الاعتماد يفحص `valid_until` تحت القفل ويرفض بـBD409. القيمة
  المخزنة `expired` محجوزة لمهمة تثبيت دورية مستقبلية (خارج الـMVP).
- **الاستبدال ودلالة المؤشر (بند 2 + اعتراض جولة التصديق 1):**
  `current_version_id` = **النسخة المواجهة للزبون** (آخر مرسلة أو مقررة)،
  أو المسودة الوحيدة إن لم يُرسل شيء بعد. القواعد الذرية:
  - `create`: ينشئ مسودة ويختم المسودة السابقة `superseded`؛ **لا يحرك
    المؤشر إلا إذا كان NULL أو يشير إلى المسودة المستبدلة** — وجود عرض
    مرسل للزبون يبقيه هو الحالي (ومنه يُشتق وضع الأب `sent` لا `draft`).
  - `send(p_version_id)`: **يقبل معرّف المسودة ولا يشترط كونها الحالية**؛
    يختم المرسلة السابقة `superseded` ويحدّث المؤشر إلى المرسلة الجديدة
    في نفس المعاملة.
  - `approve/reject(p_version_id)`: يشترطان `= current_version_id`
    وحالتها المخزنة `sent`. الاعتماد يشترط إضافةً عدم الانقضاء (BD409)؛
    الرفض جائز على المنقضية فعليًا — تسجيل واقعة تجارية.
  فهارس جزئية فريدة تفرض: **مسودة واحدة، مرسلة واحدة، معتمدة واحدة**
  كحد أقصى لكل عرض — قيود لا أعراف.
- النسخ القديمة لا تُحذف أبدًا — تبقى بحالتها التاريخية (انضباط الدفاتر).
- **الربط محركي (بند 1):** `UNIQUE (org, id, quotation_id)` على النسخ؛
  `current_version_id` بمفتاح ثلاثي يمنع نسخة من عرض آخر؛ طلب الخصم بمفتاح
  ثلاثي يمنع ربطه بنسخة لا تخص عرضه؛ **عرض أب واحد لكل مشروع**
  `UNIQUE (org, project_id)`.

العرض الأب (`quotations.status`) **مشتق حصرًا** من نسخته الحالية ولا يُكتب
إلا من RPC. المشروع: الاعتماد يحرّك `quotation → customer_approved` ذريًا
داخل RPC الاعتماد.

**ب. الصلاحيات:**

| العملية | admin | sales | field/tailor |
|---|---|---|---|
| إنشاء نسخة (والمشروع في measured أو quotation) | ✅ | ✅ | ❌ |
| إرسال نسخة | ✅ | ✅ | ❌ |
| تسجيل اعتماد/رفض الزبون (الموظف يسجّل واقعة) | ✅ | ✅ | ❌ |

**مصفوفة الخصم (بند 4 — تُنفَّذ عند الإرسال، والحدود من `business_settings`
لا أرقامًا صلبة):**

| المنفِّذ والنسبة | النتيجة |
|---|---|
| sales ≤ حد الموظف | يمرّ مباشرة |
| sales فوق حد الموظف وحتى حد الأدمن | يلزم `discount_request` معتمد |
| **admin ≤ حد الأدمن** | **يمرّ مباشرة — لا يطلب موافقة على صلاحيته هو** |
| أي أحد فوق حد الأدمن | **Override موثق حصرًا** — `discount_request` بنسبةٍ فوق الحد يعتمده أدمن (التوثيق = صف الطلب نفسه + سجل التدقيق؛ لا عمود جديد — الطلب المعتمد بنسبة > حد الأدمن هو الـOverride) |

قرار مالك 2026-08-03: صف الـOverride أعيد **حسب دليل الفريق** («ممنوع إلا
Override موثق») بعد أن كانت المسودة السابقة جعلته مرفوضًا كليًا بلا قرار —
انحراف صحّحته المراجعة. **حد الهامش الأدنى سقف مطلق لا يكسره أي مسار خصم،
Override أو غيره** (نص الدليل: «السعر النهائي لا يجوز أن ينزل تحت هامش
الربح الأدنى»).

**صلاحية موافقة الخصم مربوطة بالمحتوى (بند 4):** طلب الخصم يحمل
`content_fingerprint` (NOT NULL محركيًا) ببصمة محتوى النسخة لحظة الطلب —
**مواصفتها القانونية الملزمة في ح أدناه**؛ RPC الإرسال يعيد حسابها من
نفس الدالة الواحدة ويرفض BD409 إن تغيّر المحتوى بعد الموافقة — اخترنا
البصمة لا تجميد المسودة عند الطلب، كي لا يظهر مسبِّب تجميد ثالث يشوّش
معنى `locked`.

**ج. معنى `locked` — شكل دورة الحياة الكامل (بند 5):** القيد القديم كان
أحادي الاتجاه؛ استُبدل بـ`version_lifecycle_shape` يفرض التركيبة الكاملة
لكل حالة: المسودة غير مقفلة بلا أختام؛ المرسلة تحمل `sent_at + sent_by`
ومقفلة؛ المعتمدة/المرفوضة تحملان `decision_recorded_by` والرفض يوجب
`rejected_at + decision_note` غير فارغة؛ المستبدلة توجب `superseded_at`.
الأعمدة الجديدة وقائع تجارية: **من أرسل، ومن سجّل قرار الزبون، ومتى،
وبأي ملاحظة**. `locked` يجمّد المحتوى (حُرّاس 0014)؛ حقول دورة الحياة
تتحرك عبر RPC فقط (الجداول RPC-only أصلًا — لا منح كتابة لـauthenticated).

**د. اعتماد الزبون غير قابل للتراجع في MVP.** التصحيح قبل الاعتماد = نسخة
جديدة. بعد الاعتماد، التغيير حدثُ مشروع — خارج §10 ومسجَّل مؤجلًا. عرضٌ
اعتُمدت نسخة منه لا يقبل اعتماد أخرى (فهرس `one_approved` يفرضها) ولا
إنشاء نسخ جديدة (BD409 من RPC الإنشاء).

**هـ. محرك التسعير — المفاهيم الثلاثة منفصلة قسرًا (بند 6 + تصحيح جولة
التسعير):** المراجعة كشفت أن معادلة المسودة السابقة
(`unit_price × running_meters × quantity`) كانت **مضاعفة فعلية** على
الدلالة المخزنة: `running_meters` في النموذج الأولي وفي
`quotation_items` تتضمن الكمية أصلًا (`width_m × quantity`). الصيغة
الملزمة تفصل أساس الفوترة عن عدد القطع عن معامل الاستهلاك:

```
لكل شباك (بند مستقل — «كل شباك يُحسب مستقلًا ثم تُجمع البنود»):
billable_rm       = round3(width_m × quantity)        ← أساس الفوترة وأجور المكونات
required_fabric_m = round3(billable_rm × fabric_multiplier)   ← windows.fullness (افتراضيًا 3)
required_lining_m = has_lining ? round3(billable_rm × lining_multiplier) : 0   ← نفس fullness في v1
line_total        = round(rate_per_rm × billable_rm)  ← سعر الزبون لا يُضرب في المعامل أبدًا
cost_per_rm       = fabric_wholesale × fabric_multiplier
                  + (has_lining ? lining_wholesale × lining_multiplier : 0)
                  + tailor_cost(band) + track + delivery + measure_install
internal_cost     = round(cost_per_rm × billable_rm)
```

- **عقد التخزين:** `quotation_items.running_meters` = `billable_rm`
  (الكمية داخلها)؛ عمود `quantity` للعرض والمرجعية فقط — **لا يدخل في أي
  حساب لاحق أبدًا**.
- `required_fabric_m` و`required_lining_m` هما **كميتا الحجز من المخزون**
  (بكرات القماش والبطانة حجوزات مستقلة عبر `reserve_fabric` القائمة).
- فصل الجملة عن البيع: كلفة القماش من `fabric_variants.cost_per_meter_agorot`
  (مثال الدليل: 14×3=42 كلفةً/م جارٍ) وسعر الزبون من قواعد التسعير الثماني
  (290) — لا يختلطان.
- الخياط 40/70 بحسب الشريحة من `pricing_rules.tailor_cost_per_meter_agorot`؛
  السكة والتوصيل والقياس-والتركيب من `business_settings` — في v1 كلها مفعلة
  دائمًا (`components_enabled` في السياق كلها true؛ التعطيل الانتقائي مؤجل).
- **فوق 500 سم: لا تسعير تلقائي — `BD422` ومراجعة أدمن.** (النموذج الأولي
  كان يسعّر تلقائيًا بشريحة العالي مع تحذير فقط — انحراف عن الدليل صُحح
  في العقد.)

**قاعدة الشيكل الصحيح — قرار مالك 2026-08-04 (`calculation_version = 2`):**
**لا أغوروت في أي مبلغ إطلاقًا.** كل قيمة مالية مخزَّنة أو معروضة عدد صحيح
من الشواكل، والكسر **يُسقَط (floor) لا يُقرَّب** فلا يدفع الزبون أبدًا أكثر
مما تُظهره الحسبة. والإسقاط **عند كل مرحلة لا عند الإجمالي وحده**: لو
أُسقط في النهاية فقط لصارت بنودٌ فيها أغوروت تُجمع في إجمالي صحيح — أرقام
لا تتطابق أمام الزبون في المستند. المراحل: كل بند ← الخصم ← الإيراد الصافي
(ومنه تُشتق الضريبة فتخرج صحيحة هي الأخرى). `rounding_policy` في السياق
صار `floor_to_whole_shekel_per_stage`، والنسخ المنشأة بالنسخة 1 تحتفظ
بسياقها ولا تُمَس (المحتوى مجمّد بقرار «و»).

**التجميع والضريبة — قرار مالك 2026-08-03: الأسعار الثمانية نهائية
«شاملة الضريبة»** (الدليل صامت؛ إضافة النموذج الأولي 18% فوق السعر كانت
افتراض مبرمجين أُزيل):
```
subtotal       = Σ line_total                    ← شامل الضريبة
discount       = round(subtotal × pct / 100)
net            = subtotal − discount             ← ما يدفعه الزبون (total = net)
vat            = net − round(net / (1 + vat%/100))  ← تُستخرج داخليًا للتقارير والـPDF
revenue_ex_vat = net − vat
margin_%       = round((revenue_ex_vat − Σ internal_cost) / revenue_ex_vat × 100, 2)
```
مثال الدليل: عرض 580 شاملة → ضريبة مستخرجة 88.47 ₪، إيراد صافٍ 491.53،
هامش 35.3% — **الهامش يُقاس على الإيراد الصافي بلا ضريبة دائمًا**، وإنفاذ
الحد الأدنى (رفض الإرسال BD422) على هذا الأساس، بلا استثناء حتى مع خصم
Override معتمد.

**عقد التقريب (اعتراض جولة التصديق 5 — يستبدل ادعاء التطابق السابق):**
- **SQL بـ`numeric` الدقيق هو المرجع المالي الوحيد** — لا يُدّعى تطابق
  الطرفين لمجرد استعمال `Math.round` (ثنائية `Number` قد تنحرف عند حدود
  نصف الأغورة).
- المعاينة في TS تحسب **بأعداد صحيحة بالأغورات** (الأمتار أعدادًا صحيحة
  بالمليم-متر؛ المضروبات ضمن نطاق الأمان 2^53) بنمط النصف-بعيدًا-عن-الصفر.
- **Golden Vectors مشتركة** (`expo/domain/pricing.vectors.json`): حالات
  بمدخلات ومخرجات أغورية متوقعة يفحصها الجانبان — مجموعة SQL في بوابة CI
  و`bun test` في وظيفة TypeScript. **أي فرق أغورة = فشل بوابة.**
- الأمتار `round3` (والأعمدة `numeric(12,3)` تثبّت التمثيل).

`pricing_context` يحمل: `calculation_version=1` · `captured_at`
· **`vat_mode='inclusive'`** · `vat_percent` ·
`rounding_policy='half_away_from_zero_per_stage'` · `currency` · لقطة
القواعد الثماني · لقطة الإعدادات (تكاليف المكونات، حدود الخصم،
`min_margin_percent`، `validity_days`) · `components_enabled`.
`domain/pricing.ts` حُدّث ليطابق السياسة المقررة (معاينة فقط — SQL هو
الحجة). الحساب كله في القاعدة — الجهاز لا يرسل أرقامًا.

**و. تغيّر السعر/الضريبة بعد الإرسال: أثره صفر على المرسل.** كل المبالغ
والسياق مجمّدة في النسخة. النسخ الجديدة تلتقط القواعد الحالية. المنقضي
فعليًا يُرفض اعتماده BD409، وإعادة التقديم = نسخة جديدة.

**ز. ميكانيكا ملزمة:** ترقيم النسخ `max+1` تحت قفل صف العرض؛ ترقيم العرض
`Q-YYYY-####` عبر **`core.document_sequences` (بند 7)** — صف لكل
(مؤسسة، نوع، سنة) يُقفل `FOR UPDATE` فلا سباق حتى على العرض الأول
(`lpad` أربع خانات يتمدد طبيعيًا فوق 9999)، والسنة من
`business_settings.timezone` (IANA، الافتراضي Asia/Jerusalem) لا من
توقيت جلسة PostgreSQL.

**ترتيب الأقفال الشامل (اعتراض جولة التصديق 4):**
```
document_sequences → quotation → version(s) → project (أخيرًا دائمًا)
```
- قفل العداد لا يلزم إلا عند إنشاء العرض الأول (لا صف عرض يُقفل بعد) —
  ومن يأخذه يأخذه **قبل** أي قفل آخر.
- فحص إصدار المشروع والإسناد تحت قفل المشروع الأخير — كنمط المخزون حرفيًا.
- **موضع قفل `discount_requests` (مثبَّت قبل بناء دواله):**
  `document_sequences → quotation → version(s) → discount_request → project`.
  دالتا الطلب والقرار تقفلان العرض ثم النسخة ثم صف الطلب، والمشروع أخيرًا
  كالعادة. **الإرسال يقرأ الطلب المعتمد بلا قفل عمدًا**: القرار أحادي
  الاتجاه (pending → approved/rejected مرة واحدة، يفرضه
  `decision_is_complete`) فلا يوجد سباق يُفسد — طلب اعتُمد لا يُنقض،
  وطلب لم يُعتمد بعدُ يعيد BD403 ويُعاد الإرسال بعد الاعتماد.
  ولا يجوز لأي دالة أخذ الأقفال بترتيب مغاير لهذه السلسلة.

**عقد دالتَي الخصم (يكمل ح-2):**
- `request_discount(p_version_id, p_reason, key)` — admin+sales؛ النسخة
  **مسودة** حصرًا (البصمة تُلتقط لمحتوى قابل للإرسال)؛ النسبة تُشتق من
  `discount_percent` المخزونة في النسخة نفسها (مصدر واحد — لا نسبة من
  العميل)؛ طلبٌ لنسبة ≤ حد الموظف بالسياق المجمّد يُرفض BD400 (لا يلزم
  طلب)؛ **طلب معلّق واحد لكل نسخة** (فهرس جزئي فريد، BD409)؛ يملأ
  `content_fingerprint` من الدالة الموحدة.
- `decide_discount_request(p_request_id, p_approve, p_note, key)` —
  **admin فقط**؛ الطلب `pending` حصرًا (BD409)؛ الرفض يوجب ملاحظة غير
  فارغة BD422؛ لا إعادة فحص للبصمة هنا — فحصها الملزم عند الإرسال
  (والمسودات محتواها ثابت أصلًا: لا مسار تعديل مباشر — البصمة تحمي
  المسارات المستقبلية والصفوف المفتعلة).
- حمولتا v3:
  `'request_discount'|version_id|btrim(reason)` ·
  `'decide_discount_request'|request_id|approve|btrim(note)`.

**ح. البصمات — مواصفات قانونية ملزمة (اعتراضا جولة التصديق 2 و3):**

**ح-1. بصمة محتوى النسخة (`content_fingerprint`):** دالة واحدة
`private.version_content_fingerprint(version_id)` هي المصدر الوحيد —
يستدعيها طلب الخصم (التقاطًا) والإرسال (تحققًا):
- الخوارزمية: **SHA-256** (`pgcrypto`)، والمخرج hex صغير الأحرف (64).
- المدخل **نص قانوني محدد البايتات** لا JSON عابر للغات — حقول بفاصل `|`
  وبنود بفاصل `||` (اختير النص المرتب على JSON كي لا يعتمد التمثيل على
  ترتيب مفاتيح jsonb؛ البصمة تُحسب في SQL حصرًا في الالتقاط والتحقق معًا):
```
'fp1' | currency | calculation_version | vat_mode | vat_percent | discount_percent
      | valid_until (UTC، YYYY-MM-DD"T"HH24:MI:SS.US"Z")
      | sha256_hex(pricing_context::text)
      | البنود مرتبة (sort_order, id):
        id | width_cm | height_cm | running_meters | quantity | category | band
           | unit_price_agorot | line_total_agorot | internal_cost_agorot
           | fabric_meters | lining_meters
```
- التمثيل الرقمي تثبته أنواع الأعمدة نفسها (`numeric(10,2)`/`(12,3)`/
  `(5,2)`/`bigint`) عبر `::text` — لا تنسيق حر.
- التطبيع: uuid نصًا صغير الأحرف؛ لا نصوص حرة داخل البصمة (أسماء الغرف
  والشبابيك والوصف تجميلية خارجها عمدًا)؛ أي حقل اختياري مستقبلي NULL
  يُمثل بالحرفية `null`.
- `fingerprint_version = fp1` بادئة السلسلة — أي تغيير مستقبلي = fp2
  بترحيل مقصود.

**ح-2. بصمات الـidempotency (v3) — الحمولة القانونية لكل دالة** (بعد
التطبيع: `btrim` للنصوص، uuid صغير الأحرف، النسب بخانتين):
```
create_quotation_version : 'create_quotation_version'|project_id|discount_percent|btrim(note)
send_quotation_version   : 'send_quotation_version'|version_id
approve_quotation_version: 'approve_quotation_version'|version_id|btrim(decision_note)
reject_quotation_version : 'reject_quotation_version'|version_id|btrim(decision_note)
```
- **`p_expected_project_version` خارج البصمة** (قاعدة v3 الثابتة من §8).
- لا تواريخ ولا مبالغ من العميل في هذه الدوال إطلاقًا — الخادم يحسب
  ويختم؛ فلا حاجة لتطبيع تواريخ في الحمولات.
- الرد المخزن يُعاد حرفيًا عند التطابق، وBD409 عند نفس المفتاح بحمولة
  مختلفة — قبل أي فحص حالة (نمط §8).

دوال هذا الفرع بعد التصديق: `create_quotation_version` ·
`send_quotation_version` · `approve_quotation_version` ·
`reject_quotation_version` ثم دوال الخصم.

---

### 11. عقود الدفعات — مثبَّتة قبل بناء دوالها

اكتشف المالك أثناء التجربة أنه يستطيع تسجيل دفعة **قبل** وجود عرض معتمد.
هذا خطأ محاسبي لا تجميلي: بلا عرض معتمد **لا يوجد مبلغ متفق عليه** تُقاس
عليه الدفعة، فتنشأ «مستحقات» و«متبقٍ للتحصيل» بلا أصل، وتتلوث تقارير
الإيراد بأرقام لا سند لها.

**القاعدة الملزمة:** لا دفعة على مشروع ليس له نسخة عرض `approved`.
الرفض `BD409` من `api.record_payment` حين تُبنى — والحارس في التطبيق
(اليوم) طبقة تجربة مستخدم لا بديلًا عن المحرك، تمامًا كما في بقية الطبقات:
النموذج نفسه لا يُعرض قبل الاعتماد، فيُمنع الفعل بدل أن يُرفض بعد كتابته.

بنود تُحسم مع بناء الطبقة (مسجَّلة كي لا تُنسى):
- سقف مجموع الدفعات: هل يُمنع تجاوز إجمالي العرض المعتمد أم يُسمح بفائض
  يُعاد لاحقًا؟
- أثر الاعتماد اللاحق لنسخة أعلى/أقل قيمة على دفعات سابقة.
- `reverse_payment`: قيد عكسي دائم لا حذف (نمط دفتر الأستاذ في §8).

## قرارات مؤجَّلة تحتاج حسمًا لاحقًا

- **حذف الحساب داخل التطبيق** — آبل وجوجل يشترطانه، ويتعارض ظاهريًا مع قاعدة
  «لا حذف فعلي». يلزم سياسة: إخفاء الهوية؟ حذف مؤجَّل بمهلة احتفاظ؟
- **تحليلات PostHog** المحقونة عبر `RorkAnalyticsProvider` — تبقى مع الإفصاح في
  نماذج الخصوصية، أم تُزال مع إعادة بناء polyfills الويب؟
- **واجهة الويب** — Next.js منفصل كما يقترح الدليل، أم تصدير Expo Web لنفس الكود؟
- **تعدد المؤسسات** — محل واحد، أم منتج SaaS يُباع لمحلات ستائر متعددة؟
