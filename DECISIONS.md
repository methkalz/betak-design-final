# قرارات معمارية — بيتك ديزاين

قرارات مثبَّتة تحكم كل عمل لاحق. أي انحراف عنها يحتاج تحديث هذا الملف أولًا.

---

## 2026-08-02

### 1. مسار الكتابة — هجين محدَّد

**القراءة** عبر views آمنة حسب الدور. **الكتابة البسيطة** مباشرة على الجداول
المسموح بها مع RLS وقيود صارمة. **العمليات الحساسة أو متعددة الخطوات** عبر
RPC transactional حصرًا.

| كتابة مباشرة مع RLS | RPC حصرًا |
|---|---|
| إنشاء وتعديل الزبون | حجز القماش وتحريره |
| تعديل عنوان أو ملاحظة | الاستهلاك والإرجاع والتلف |
| إنشاء مشروع كمسودة | أي تعديل في رصيد المخزون |
| إضافة غرفة أو شباك | إنشاء واعتماد نسخة عرض سعر |
| إدخال قياسات | طلب واعتماد الخصم |
| إضافة موعد أو ملاحظة | تسجيل دفعة أو عكسها |
| أي بيانات غير مالية وغير مخزنية | قبول العرض وتفعيل المشروع |
| | إغلاق أو إلغاء المشروع |
| | أي عملية تمس عدة جداول وتجب كوحدة واحدة |

**قيد تقني اكتُشف عند التنفيذ:** PostgREST يعرض سكيما `api` وحدها، فـ`core`
غير موجودة في سطح الـAPI. لذا «الكتابة المباشرة على الجدول» تتحقق عبر **view
تمريري 1:1** بلا joins مع `security_invoker` — يجعله بوستجرس قابلًا للكتابة
تلقائيًا، فالتطبيق يكتب عليه وكأنه الجدول وتنطبق RLS والقيود كما هي. هذا ليس
Writable View بالمعنى المعقّد، بل ممر شفاف.

**اصطلاح التسمية:**
- `api.<entity>` — تمريري 1:1، **قابل للكتابة**
- `api.<entity>_details` — مثرى بـjoins، **قراءة فقط**
- `api.<entity>_summary` — تجميعي، قراءة فقط
- `api.<entity>_financials` — أعمدة التكلفة والربح، محكوم بشرط دور

قاعدة ثابتة: **view الخياط لا يحتوي التكلفة أو الربح أصلًا** — لا مخفية ولا مصفّرة.

### 2. المصادقة — بريد + كلمة سر

Supabase Auth بالبريد وكلمة السر. الأدمن ينشئ الحسابات ويوزّعها. بلا تكلفة
وبلا مزود خارجي. يمكن إضافة الهاتف لاحقًا دون كسر شيء.

`Profile.pin` في النموذج الأولي **محذوف نهائيًا** — رقم سري في جدول ليس مصادقة.

الجلسة تُحفظ في **SecureStore** (Keychain / Keystore) لا في AsyncStorage.

### 3. العمل دون إنترنت — للميدان فقط

القياس والتركيب والصور تعمل دون شبكة وتُزامَن لاحقًا عبر طابور.

المال والمخزون والخصومات **تتطلب اتصالًا ولا تُطبَّق تفاؤليًا أبدًا** — لا تُعتبر
مؤكدة قبل نجاح الـRPC. هذا نص الدليل حرفيًا.

كل عملية حساسة تحمل `client_operation_id` و `idempotency_key` و `lock_version`.

### 4. هوية التطبيق — `com.baytakdesign.app`

| | |
|---|---|
| iOS `bundleIdentifier` | `com.baytakdesign.app` |
| Android `package` | `com.baytakdesign.app` |
| الرابط العميق `scheme` | `baytakdesign` |

يحل محل `app.rork.72xauvl81ef7xdo88xgur` المولَّد. **يجب أن يُثبَّت قبل أول رفع
للمتجر** — بعده يستحيل تغييره عمليًا.

### 5. شاشة الخياط — قرار مقصود لا نتيجة عرضية

`api.tailor_project_details` **لا يحوي**: التكلفة، الربح، الخصم، اسم الزبون،
هاتفه، عنوانه. اختبار آلي (18b) يتحقق من غياب هذه الأعمدة من **تعريف** الview.

لكن الخياط يحتاج تمييز الرزم عمليًا، فالview يحمل:
`code` (مثل `BD-1041`) و `title` و `window_count` و `total_fabric_meters`
و `installation_date`.

`title` نص حر يكتبه الموظف، وقد يضع فيه اسم الزبون («بيت مثقال — كفر قاسم»).
هذا **مقبول ومقصود**: الاسم ليس سرًّا ماليًا، والخياط يحتاجه للتمييز. أما ما
يبقى محجوبًا فعلًا فهو الهاتف والعنوان والأرقام المالية.

إن أردنا خصوصية أعلى لاحقًا، الحل تقييد `title` أو إضافة
`project_display_name` منفصل — لا تغيير الview.

### 6. مالك دوال RPC — `baytak_rpc_owner`

دور `nologin` بـ`bypassrls`، يملك دوال `api` الحساسة بدل `postgres`.
`bypassrls` لازم لأن `FORCE ROW LEVEL SECURITY` يشمل المالك وكل سياساتنا
`to authenticated`. الحدّ الحقيقي هو **الصلاحيات الجدولية المسمّاة**: تجاوز
الصفوف لا يخلق صلاحية على جدول لم يُمنح عليه شيء.

قيود اكتُشفت عمليًا على هذا البناء (`supabase/postgres:15.8.1.085`):

| القيد | التفصيل |
|---|---|
| `GRANT role TO current_user` | **يُسقط الخادم** — خلل في supautils. استخدم الاسم الصريح. |
| PostgreSQL 15 | مُنشئ الدور لا يصير عضوًا تلقائيًا (تغيّر في 16)، فالمنح إلزامي قبل `ALTER … OWNER`. |
| `ALTER … OWNER TO` | تشترط `CREATE` على السكيما للمالك الجديد، لا `USAGE` وحدها. |
| سكيما `auth` | مملوكة لـ`supabase_admin`، و`postgres` يحمل `USAGE` بلا `GRANT OPTION` → لا يمكن تمريرها. الحل `private.current_uid()` بدل `auth.uid()`. |
| `SELECT … FOR UPDATE` | تتطلب صلاحية `UPDATE` لا `SELECT` فقط، حتى دون تعديل الصف. |

### 7. المخطط التصريحي — نموذج هجين

`supabase/schemas/` مصدر الحقيقة للبنية، و`supabase/migrations/` السجل
التاريخي غير القابل للتعديل. الاثنان معًا لا أحدهما.

**السبب ليس تنظيميًا:** توثيق Supabase يذكر صراحةً أن `db diff` لا يتتبّع
`security_invoker`، والـ39 view عندنا تعتمد عليه — بدونه تتجاوز RLS كليًا.
كذلك لا يتتبّع الملكية والمنح وصلاحيات الأعمدة والسكيمات والتعليقات و DML.
فتلك كلها تبقى في migrations يدوية.

**دوال قاعدة البيانات في `supabase/schemas/` لا في `supabase/functions/`** —
الأخير محجوز لـEdge Functions.

**الترتيب اعتمادي لا أبجدي.** اكتُشف عمليًا: `can_see_project` تسبق `role_in`
أبجديًا وتستدعيها، و`inventory_roll_balances` تعتمد على `roll_balances`،
وسياسات RLS تستدعي `has_role`. الترتيب مفروض في `config.toml` صراحةً —
**لا تستبدله بـ`./schemas/*.sql`**.

**التحقق:** `supabase/tests/schema_drift.py` يبني قاعدة مؤقتة من الملفات
ويقارن بصمة الكتالوج مع الحية. آخر نتيجة: 709 كائنًا متطابقًا، NO DRIFT.

### 8. محاسبة المخزون — المرجع المركزي وسياسة الـenum

**`core.movement_effects` هو المصدر الوحيد لأثر الحركات.** كل رصيد يُحسب
`sum(quantity_m × sign)` بالانضمام إليه — ممنوع تضمين مصفوفة أثر يدوية في أي
view أو RPC أو اختبار. (الاختبار نفسه وقع في هذا الفخ: نسخة يدوية قديمة فيه
أعطت نتائج خاطئة بعد إضافة النوع الجديد — فأُصلح ليشتق من الجدول.)

| الحركة | on_hand | reserved |
|---|---|---|
| receipt / return / adjustment_in / transfer_in | + | 0 |
| reservation | 0 | + |
| reservation_release | 0 | − |
| **consumption** | − | − |
| **overconsumption** | − | 0 |
| damage (من المتاح) / adjustment_out / transfer_out | − | 0 |

**سياسة enum:** قيم `movement_type` تُضاف فقط — لا يُعاد تسميتها ولا تُحذف
(حذف قيمة enum في PostgreSQL شبه مستحيل). لذا بقيت `consumption` باسمها
وأُضيفت `overconsumption` — الزيادة عن الحجز لم تعد `adjustment_out` فلا
تختلط بتصحيحات الجرد في تقارير الهدر.

**`operation_group_id`** يربط حركات الإجراء الواحد (استهلاك 30 + زيادة 5)،
ويُولَّد داخل الـRPC حصرًا — لا يُقبل من الجهاز.

**invariant الحجز** (يفرضه قيد على المحرّك، وسّعناه قبل بناء RPC التلف لا بعده):

```
quantity_m = consumed_m + released_m + damaged_reserved_m + remaining
```

والصيغ محصورة في مكانين: `private.reservation_remaining` و `private.roll_balance`.

**قواعد idempotency الملزمة** (بعد مراجعة كود كشفت فجوتين):
- البصمة القانونية تشمل **كل مدخل يؤثر فيما يُكتب** — الكمية والسبب
  والملاحظات، لا الكمية وحدها. نفس المفتاح ببصمة مختلفة → BD400.
- `p_expected_project_version` **ليس** جزءًا من البصمة: شرط تزامن لا هوية عملية.
- ترتيب ملزم في كل RPC: هوية وعضوية ودور ← بصمة ← فحص idempotency
  (إعادة النتيجة إن طابقت) ← **ثم** فحوص الحالة والإصدار للعمليات الجديدة
  فقط. إعادة إرسال طلبٍ نجح يجب أن تعيد نتيجته حتى لو تغيّر المشروع بعده.
- شكل البصمة v2 منذ 0027 — عمليات ما قبله لا يُعاد تشغيلها بمدخلات v2.

**دلالة `fabric_usage`:** الصف = **حدث استهلاك** لا ملخص حجز.
`planned_m` = الجزء المغطى بالحجز في هذا الحدث، فيصح لكل صف
`actual_m = planned_m + waste_m`، و`Σplanned ≤ quantity_m` للحجز.
(كانت «المتبقي قبل العملية» فيتضخم المجموع مع الاستهلاك الجزئي المتكرر.)

**قرار مؤجَّل لـRPC التلف:** تلف الكمية المحجوزة — إما قيمة enum جديدة
`damage_reserved` (أثر −/−) أو حركتا `damage` + `reservation_release`
بمجموعة واحدة. يُحسم عند بناء `record_fabric_damage`. المركزية تحصر
**الأثر المحاسبي** في `movement_effects`، لكن القرار يمس أيضًا الـRPC
والتدقيق والتسميات والاختبارات وأنواع TypeScript إن أُضيفت قيمة enum.

---

## قرارات مؤجَّلة تحتاج حسمًا لاحقًا

- **حذف الحساب داخل التطبيق** — آبل وجوجل يشترطانه، ويتعارض ظاهريًا مع قاعدة
  «لا حذف فعلي». يلزم سياسة: إخفاء الهوية؟ حذف مؤجَّل بمهلة احتفاظ؟
- **تحليلات PostHog** المحقونة عبر `RorkAnalyticsProvider` — تبقى مع الإفصاح في
  نماذج الخصوصية، أم تُزال مع إعادة بناء polyfills الويب؟
- **واجهة الويب** — Next.js منفصل كما يقترح الدليل، أم تصدير Expo Web لنفس الكود؟
- **تعدد المؤسسات** — محل واحد، أم منتج SaaS يُباع لمحلات ستائر متعددة؟
