# سجل الإجراءات — بيتك ديزاين

## 2026-08-03

- **فرض بقية invariants المحاسبة (ترحيل 0028)** — مراجعة ثانية وجدت قيدين
  موثَّقين غير مفروضين وتناقضًا في اختبار، والثلاثة تُحقق منها على المحرك
  الحي قبل الإصلاح:
  1. `reservation_required` لم يشمل `overconsumption` — زيادة يتيمة بلا حجز
     كانت ممكنة من مسار مرتفع الصلاحية. القيد وُسِّع، مع اختبار إدراج مباشر
     كـpostgres يجب أن يفشل.
  2. معادلة `fabric_usage` (actual = planned + waste) كانت التزام كود لا قيد
     محرك — أُضيف CHECK بمساواة دقيقة (الأعمدة numeric(12,3) فالجمع exact،
     لا حاجة لـround).
  3. مصفوفة أثر يدوية بقيت في `reserve_fabric.py` بلا `overconsumption` —
     استُبدلت بـjoin على `movement_effects` كما تقضي القاعدة المعمارية.
  النتائج: consume/release **56/56**، reserve 16/16، HTTP 5/5، RLS نظيفة،
  NO DRIFT. قراران حُسما في DECISIONS §8: `damage_reserved` كحركة مستقلة
  (التلف ليس تحريرًا)، وعقد الإرجاع المقيَّد بسجل استهلاك وسقف صارم.

- **تقسية idempotency وسلامة المحاسبة (ترحيل 0027)** — استجابة لمراجعة كود
  كشفت ثلاث فجوات لم تلتقطها الاختبارات الخضراء (54 اختبار consume/release
  و16 reserve بعد الإصلاح، منها 10 جديدة مستقلة):
  1. البصمة القانونية صارت تشمل السبب والملاحظات — نفس المفتاح بسبب مختلف
     كان يُعامل كإعادة مطابقة ويعيد النتيجة القديمة؛ الآن BD400.
  2. فحص idempotency صار **قبل** فحوص الحالة والإصدار — إعادة إرسال طلب نجح
     وضاع ردّه كانت ترجع BD409 إن تغيّر المشروع بعده؛ الآن تعيد النتيجة
     المخزنة (جوهري للعمل الميداني دون إنترنت).
  3. FK من `stock_movements.type` إلى `movement_effects` — قيمة enum بلا صف
     أثر كانت ستُسقط حركاتها من الأرصدة صامتة؛ الآن يفشل الإدراج بصوت عالٍ،
     مع اختبار تغطية يطابق قيم الـenum مع صفوف الجدول.
  4. دلالة `fabric_usage.planned_m` صُححت إلى «الجزء المغطى بالحجز لكل حدث»
     — كانت «المتبقي قبل العملية» فيتضخم المجموع مع الاستهلاك الجزئي
     (حجز 20: استهلاك 8 ثم 12 سجّل planned = 32).

- **دفع الالتزام التأسيسي والوسم** — `99f6f39` و `db-baseline-v0.1.0` إلى
  `origin/main`. قرار صاحب المشروع: يبقى المستودع عامًا مؤقتًا (مع التوضيح أن
  المستودعات العامة قابلة للاكتشاف آليًا — لا يوجد «عام غير مُدرَج» في GitHub).
- **فرع `feat/inventory-overconsumption`** — ترحيلان (0025–0026):
  - قيمة enum جديدة `overconsumption` في ملف مستقل — القاعدة تمنع استخدام
    قيمة enum في نفس معاملة إضافتها، وترحيلاتنا single-transaction.
  - `core.movement_effects` — المرجع المركزي لأثر الحركات؛ أزال 5 نسخ يدوية
    من مصفوفة الأثر (views + أجسام RPC).
  - `operation_group_id` على دفتر الحركة + فهرس جزئي — يُولَّد في الـRPC حصرًا.
  - `damaged_reserved_m` على الحجوزات + توسيع قيد الـinvariant **قبل** بناء
    RPC التلف.
  - `private.roll_balance` و `reservation_remaining` — الصيغتان الوحيدتان.
  - إعادة الـRPCs الثلاثة على المصادر المركزية. لا `grant create` هذه المرة:
    postgres عضو في المالك و`CREATE OR REPLACE` يحفظ الملكية والصلاحيات.
  - أنواع Expo محدَّثة (`overconsumption`، `releasedM?`، `damagedReservedM?`،
    `operationGroupId?`) — `tsc --noEmit` نظيف. حقلا الحجز اختياريان عمدًا
    لتوافق البيانات المحفوظة على الأجهزة قبل هذا التغيير.
  - النتائج: consume/release **44/44** (منها 7 جديدة)، reserve 15/15،
    HTTP 5/5، RLS صامتة، tsc نظيف، **NO DRIFT** بعد إعادة توليد schemas
    من القاعدة الحية (18 دالة الآن).
  - 🟠 درس: نسخة يدوية قديمة من مصفوفة الأثر داخل ملف الاختبار نفسه أعطت
    نتائج خاطئة بعد إضافة النوع الجديد — أُصلحت لتشتق من `movement_effects`.
    القاعدة تسري على الاختبارات أيضًا، لا على كود الإنتاج فقط.

كل تغيير على الـVPS يُسجَّل هنا سطرًا واحدًا (التاريخ، ماذا، ولماذا) ليتمكن المشغّل
التالي من تدقيقه. مطلوب بموجب §7 من دليل تشغيل الـVPS.

---

## 2026-08-02

- **إنشاء نسخة Supabase مستقلة `baytak`** — عبر Coolify API، `instant_deploy:false`.
  معرّف النسخة `se3nf6ovhfq3qr3sjxzi9yba`. السبب: عزل كامل لمشروع بيتك ديزاين
  (قاعدة + Auth + Storage + PostgREST خاصة به) بدل مشاركة ستاك قائم.
- **تثبيت دومين kong** على `https://supabase-baytak.qinova.link:8000` **قبل** أول
  نشر. السبب: توليد وسوم Traefik صحيحة من أول إقلاع وتفادي الـ503 المعروف.
- **نشر النسخة** عبر `/api/v1/deploy?force=true`. النتيجة: 14 حاوية سليمة،
  و`/auth/v1/health` يعيد 401 (أي أن Kong يوجّه عبر TLS بنجاح).
- **تخزين رمز Coolify** في `/root/.coolify_env` بصلاحية 600. السبب: منع ظهور
  الرمز في قائمة العمليات أو في سجل الصدفة. ملاحظة: الرمز بصيغة Sanctum ويحوي
  `|`، فيجب اقتباسه بعلامات مفردة عند التصدير وإلا فسّرته الصدفة كأنبوب.
- **تطبيق 10 ترحيلات** على قاعدة النسخة: 28 جدولًا، 19 نوعًا، 99 فهرسًا،
  77 مفتاحًا خارجيًا، 76 قيد تحقق، 6 دوال `private`، و48 سياسة RLS.
  كل ملف طُبِّق بـ `ON_ERROR_STOP=1 --single-transaction`.
- **إنشاء دفتر الترحيلات** `supabase_migrations.schema_migrations` وتسجيل
  النسخ العشر فيه. السبب: توافق مع `supabase db push` مستقبلًا دون إعادة تشغيلها.
- **إعادة تسمية ملفات الترحيل** إلى صيغة الطابع الزمني `202608021200NN_*.sql`.
  السبب: صيغة `0001_` لا يتعرف عليها Supabase CLI.
- **تسجيل غلاف MCP** `/usr/local/bin/supabase-mcp-baytak` عبر
  `/opt/supabase-mcp/new-client.sh`. الموزّع يسمح به تلقائيًا (لا تعديل على
  `authorized_keys`).
- **بناء صورة `selfhosted-supabase-mcp:latest`** من `Dockerfile.fixed` —
  ⚠️ **إصلاح لعطل قائم على مستوى المنصة كلها**، انظر أدناه.
- **التحقق من التوجيه** بلا إعادة تشغيل: `get_project_url` أعاد
  `https://supabase-baytak.qinova.link`، و`list_tables` رأى جداول `core`.

- **عرض سكيما `api` لـPostgREST** — `PGRST_DB_SCHEMAS` صار
  `public,storage,graphql_public,api`. التعديل تم عبر **Coolify API** لا بالكتابة
  في جدوله: القيمة مشفَّرة بمفتاح Laravel، والكتابة المباشرة كانت ستفسدها.
  ثم نشر لإعادة توليد الـcompose. تحقق بعده: القيمة في الحاوية العاملة وفي
  `docker-compose.yml` (السطران 126 و398 — `supabase-rest` و`supabase-studio`)،
  والبيانات سليمة (28 جدولًا، 48 سياسة، 6 دوال، 10 ترحيلات).
  السبب: بدون هذا لا يصل التطبيق إلى سكيما `api` إطلاقًا مهما كُتب فيها.

- **طبقة `api`** (ترحيل 0011): 33 view كلها `security_invoker = on`. الأعمدة
  المالية غائبة من الviews الأساسية، وفي views منفصلة تحمل شرط `WHERE` على
  الدور — الشرط ضروري لأن RLS تحت `fabric_variants` تسمح لكل الأعضاء بالقراءة.
- **إلغاء صلاحيات مستوى العمود** على `business_settings` (كانت في 0010).
  Supabase توصي صراحة بتجنبها: تكسر `SELECT *` عبر PostgREST، ولا تميّز بين
  مستخدم وآخر لأن الجميع يستعمل دور `authenticated`.
- **اختبار RLS حي** بخمسة مستخدمين ومؤسستين — 23 سيناريو، كلها داخل transaction
  ينتهي بـ`ROLLBACK`. محفوظ في `supabase/tests/rls_policies.sql`.

- **سطح الكتابة وحُرّاس السلامة (ترحيل 0014)** — تنفيذًا لقرارات
  [DECISIONS.md](DECISIONS.md) §1:
  - منح `INSERT/UPDATE` على 14 جدولًا يقبل الكتابة المباشرة. بدونها كانت
    سياسات الكتابة في 0010 **كودًا ميتًا**: السياسة تسمح والصلاحية غائبة.
  - views تمريرية 1:1 قابلة للكتابة (`api.projects` و `api.windows` و
    `api.field_visits` و `api.tailor_assignments`)، والإثراء انتقل إلى
    `_details`. السبب تقني: بوستجرس لا يجعل الview قابلًا للكتابة إن كان فيه join.
  - `api.tailor_project_details` — شاشة الخياط بلا أي عمود تكلفة أو ربح أو زبون.
  - 8 triggers: دفاتر الأستاذ الثلاثة غير قابلة للتعديل، `status_code` عبر RPC
    حصرًا، `lock_version` يرتفع تلقائيًا ويرفض الكتابة بنسخة قديمة، النسخة
    المقفلة لا تُعدَّل ماليًا (لكن الاعتماد مسموح)، ولا حذف فعلي للزبائن والمشاريع.
- **مجموعة الاختبار وسّعت إلى 34 سيناريو** — كلها ناجحة.

- **تقسية (ترحيل 0015)** استجابةً لمراجعة مهنية:
  - `revoke usage on schema core from authenticated` — **تحقق تجريبي قبل
    التطبيق**: القراءة والكتابة عبر views بـ`security_invoker` تعملان بعده،
    بينما `select from core.customers` مباشرة تُرفض بـ42501. مرجع الجدول داخل
    الview محلول إلى OID فلا يُعاد فحص USAGE.
  - نزع `DELETE` صراحةً حيث الأرشفة هي القاعدة (طبقة أسبق من الـtrigger).
  - عمود `payload` على `client_operations` لبصمة الطلب.
  - دور `baytak_rpc_owner` يملك دوال RPC بدل `postgres`.
- **`api.reserve_fabric` (ترحيلات 0016–0019)** — 15 خطوة بالعقد المتفق عليه،
  ترجع لقطة رصيد لا `void`. **15/15 اختبار قبول ناجح**، منها اختبار تزامن
  باتصالين متوازيين: 15 م لكلٍّ من رصيد 15 م → واحد نجح والآخر رُفض بنقص
  الرصيد، والرصيد لم يصر سالبًا، وحركتا حجز فقط.
  الاختبار في `supabase/tests/reserve_fabric.py`.

- **تقسية أقل صلاحية (ترحيل 0020)** — سحب `CREATE` على `api` من مالك الـRPC
  (تُمنح مؤقتًا عند كل نقل ملكية ثم تُسحب)، واستبدال `UPDATE` على جدول
  `fabric_rolls` بـ`update (retired_at)` وحده. **مُتحقَّق منه**: صلاحية العمود
  الواحد تكفي لـ`SELECT … FOR UPDATE`، و`reserve_fabric` بقيت 15/15 بعدها.
- **`api.consume_fabric` و `api.release_reservation` (ترحيلات 0021–0024)** —
  **38/38 اختبار ناجح** في `supabase/tests/consume_and_release.py`، شاملة
  الدلالات المحاسبية والـinvariants والتزامن الحقيقي والتراجع عند فشل التدقيق.
- **`KNOWN_ISSUES.md` و `supabase/tests/reproduction/`** — سكربتات إعادة إنتاج
  الأعطال البيئية خارج مجلد الترحيلات، مع أرقام إصدارات البيئة.

- **إعادة تنظيم إلى مخطط تصريحي** — `supabase/schemas/` (17 ملفًا) مُولَّدة من
  القاعدة الحية عبر `pg_get_functiondef` و `pg_get_viewdef` و `pg_dump`، لا من
  الذاكرة ولا من الترحيلات. مع `config.toml` بـ`schema_paths` صريح.
  **لم تُمَس القاعدة ولا أي migration منشورة** — استخراج فقط.
  تحقق: 15/15 و 38/38 و RLS بلا أخطاء، و**709 كائن كتالوج متطابق (NO DRIFT)**.
- **`supabase/tests/schema_drift.py`** — فحص drift بلا حاجة إلى Supabase CLI:
  ينشئ قاعدة مؤقتة على نفس العنقود، يطبّق ملفات المخطط، يقارن بصمة الكتالوج
  مع الحية، ثم يحذفها.

### 🟠 مشكلتا ترتيب كشفهما بناء المخطط التصريحي

الفرز الأبجدي — وهو سلوك `./schemas/*.sql` الافتراضي — يفشل في ثلاثة مواضع:

| الاعتماد | الخطأ لولا الترتيب |
|---|---|
| `private.can_see_project` → `private.role_in` | `function private.role_in(uuid) does not exist` |
| `api.inventory_roll_balances` → `api.roll_balances` | `relation api.roll_balances does not exist` |
| سياسات RLS → `private.has_role` | `function private.has_role(...) does not exist` |

حُلّت بترتيب اعتماديات صريح للدوال، وفرز طوبولوجي (Kahn) للviews مشتق من
`pg_depend`، وفصل السياسات إلى ملف يلي دوال `private`.

### 🔴 عيوب اكتشفها بناء دالتَي الاستهلاك والتحرير

1. **التحرير الكامل كان مستحيلًا.** `release_reservation` كانت تُنقص
   `quantity_m`، فالتحرير الكامل يجعلها صفرًا ويصطدم بـ`check (quantity_m > 0)`.
   كشفه اختبار التزامن حرفيًا. والأسوأ أنه يُفقد «المحجوز الأصلي» فينهار
   الـinvariant `reserved_initial = consumed + released + remaining`.
   **الحل: عمود `released_m` مستقل** و`quantity_m` ثابتة، مع قيد
   `consumed_m + released_m <= quantity_m` يفرض الـinvariant على المحرّك
   (ترحيل 0022).
2. **حساب المتبقي في الاستهلاك أهمل المحرَّر** بعد إضافة العمود (ترحيل 0023).
3. **خطأ نسخ**: استعلام زائد يُسند `status_code` النصي إلى متغير عددي، فسقطت
   `consume_fabric` عند كل نداء (ترحيل 0024). الدرس: إعادة كتابة دالة كاملة
   لتغيير سطر واحد مصدر خطأ متكرر — المرحلة القادمة تنقل تعريفات الدوال إلى
   ملفات تُعاد بالكامل بشكل idempotent.
4. **حقن فشل التدقيق في الاختبار كان معطوبًا**: `ALTER TABLE ADD CONSTRAINT`
   يتحقق من الصفوف القائمة فيفشل، فلم يُحقن أي فشل والاختبار مرّ كاذبًا.
   الصواب trigger شرطي.

### 🔴 أربعة عيوب اكتشفها بناء الـRPC — أُصلحت

1. **`GRANT baytak_rpc_owner TO current_user` أسقط الخادم مرتين.** عزلنا العبارة
   بتنفيذ بند بند. السبب إضافة `supautils` التي تعترض `GRANT ROLE` ولا تعالج
   `CURRENT_USER` كمستقبِل. الاسترداد كان تلقائيًا ونظيفًا (WAL) بلا فقد
   بيانات — الدفتر بقي سليمًا. **الحل: الاسم الصريح `to postgres`.**
2. **`ALTER … OWNER TO` تشترط `CREATE` على السكيما** للمالك الجديد، لا `USAGE`.
3. **`auth.uid()` غير متاحة لمالك الـRPC.** سكيما `auth` مملوكة لـ
   `supabase_admin` و`postgres` يحمل `USAGE` **بلا GRANT OPTION**، فمحاولة
   المنح تصدر `WARNING: no privileges were granted` — تحذيرًا لا خطأً، فمرّ
   الترحيل 0017 بنجاح ظاهري بلا أثر. **الحل: `private.current_uid()`** تقرأ
   الـGUC مباشرة بلا اعتماد على سكيما auth (ترحيل 0018).
4. **`SELECT … FOR UPDATE` تتطلب صلاحية `UPDATE`** لا `SELECT` فقط، حتى دون
   تعديل الصف. `FOR SHARE` يكفيه `SELECT` لكنه لا يسلسل فينهار الغرض (ترحيل 0019).

### 🔴 ثلاثة عيوب اكتشفها الاختبار الحي — أُصلحت

1. **`private` بلا EXECUTE (ترحيل 0012)** — الأخطر. الترحيل 0010 نزع صلاحيات
   سكيما `private` عن `authenticated` بنية الدفاع في العمق، لكن بوستجرس يقيّم
   تعبيرات RLS **بصلاحيات الدور المستعلِم**، وكل سياسة تنادي `is_org_member`.
   النتيجة: `ERROR 42501` على كل استعلام لأي مستخدم — **القاعدة كانت معطّلة
   كليًا**. لم يظهر العطل في الفحص الأول لأنه جرى بدور `postgres` الذي يتجاوز
   RLS. الحدّ الأمني الصحيح هو عدم عرض `private` في `PGRST_DB_SCHEMAS`.

2. **`api.projects` بـINNER JOIN على الزبائن (ترحيل 0013)** — الخياط بلا سياسة
   قراءة على الزبائن، فالـjoin محا صفوفه كلها: رأى **صفر مشاريع** بدل مشروعه.
   أي INNER JOIN إلى جدول محكوم بـRLS داخل view بـ`security_invoker` هو فلتر
   صامت. الإصلاح `LEFT JOIN` — المشروع يظهر وبيانات الزبون فارغة، وهو السلوك
   المطلوب أصلًا.

3. **`api.fabric_rolls` برصيد صفري مضلّل (ترحيل 0013)** — دفتر الحركة محكوم
   بـadmin/sales، فكان `coalesce(...,0)` يُظهر للميداني «متاح 0» و«مخزون منخفض»
   عن رول ممتلئ. فُصلت الأرصدة إلى `api.fabric_roll_stock` المحكوم.

### 🔴 عطل منصّي كان قائمًا — أُصلح

صورة `selfhosted-supabase-mcp:latest` **لم تكن موجودة على السيرفر إطلاقًا**، ولا
يمكن سحبها (المستودع غير عام). وبما أن كل الأغلفة تشير إليها، فإن **MCP كان
معطّلًا لكل النسخ** — `qinova1` و`test` و`faisal` — لا لمشروعنا وحده. الغلاف
يمرر stderr إلى `/dev/null`، فكان العطل صامتًا تمامًا: اتصال ينجح ثم لا مخرجات.

السبب الجذري: `Dockerfile` الأصلي يستخدم `npm install` مع `package-lock.json`
غير موجود (المشروع يستعمل `bun.lock`)، و`bun build --target bun` ينتج مخرجات لا
يشغّلها node. كان `Dockerfile.fixed` مكتوبًا منذ 28 أبريل لكنه لم يُبنَ قط.

البناء إجراء **إضافي** لم يمسّ أي نسخة قائمة، ويُفترض أن يستعيد MCP للجميع.

### ملاحظات تشغيلية للمشغّل التالي

- `supabase2.qinova.link` (نسخة `test`) تعيد **503** — عطل قائم سابق لهذا العمل،
  سببه غالبًا وسوم Traefik قديمة. يُصلَح بخطوتَي 3–4 من Runbook A.
- شهادة `*.qinova.link` سليمة (16 يونيو ← 14 سبتمبر 2026). أي خطأ
  `CERTIFICATE_VERIFY_FAILED` من ويندوز سببه مخزن الجذور المحلي، لا السيرفر —
  نفّذ نداءات Coolify API من الـVPS.
- رمز Coolify ظهر في رسالة خطأ أثناء هذا العمل. يُستحسن تدويره من لوحة Coolify.
