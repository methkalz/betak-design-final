# مشكلات معروفة في البيئة — بيتك ديزاين

أعطال في الأدوات لا في كودنا، مع الالتفاف المُتحقَّق منه. **أعِد اختبار كل بند
عند ترقية البيئة** — قد يزول العطل فيصير الالتفاف دَيْنًا لا داعي له.

---

## BD-ENV-001 — `GRANT role TO CURRENT_USER` يُسقط خادم قاعدة البيانات

**الخطورة:** عالية — انهيار كامل للـbackend ودخول في وضع الاسترداد.

**البيئة:**

| | |
|---|---|
| صورة القاعدة | `supabase/postgres:15.8.1.085` |
| PostgreSQL | 15.8 |
| Coolify | 4.1.2 |
| المشتبه به | `supautils` (يعترض عبارات `GRANT ROLE` لفرض `reserved_memberships`) |
| تاريخ الرصد | 2026-08-02 |

**العبارة القاتلة:**

```sql
grant baytak_rpc_owner to current_user;
```

**الأثر:** `server closed the connection unexpectedly` ثم
`the database system is in recovery mode` لجميع الخدمات. الاسترداد تلقائي من
WAL خلال ثوانٍ، **بلا فقد بيانات** — دفتر الترحيلات بقي سليمًا في الحالتين.
أُعيد إنتاج العطل مرتين، وعُزل بتنفيذ العبارات بندًا بندًا.

**الالتفاف المُتحقَّق منه — استخدم الاسم الصريح:**

```sql
grant baytak_rpc_owner to postgres;
```

**لماذا نحتاج المنح أصلًا:** في PostgreSQL 15 لا يصير مُنشئ الدور عضوًا فيه
تلقائيًا (تغيّر هذا في 16)، و`ALTER … OWNER TO` تشترط العضوية. على PG 16+
قد يزول الاحتياج كله.

**إعادة الإنتاج:** `supabase/tests/reproduction/BD-ENV-001_grant_current_user_crash.sql`
⚠️ **لا تُشغَّل على قاعدة إنتاج.** ولا تُدرَج في `supabase/migrations/`.

---

## BD-ENV-002 — منح صلاحية على سكيما `auth` يفشل صامتًا

**الخطورة:** متوسطة — ترحيل ينجح ظاهريًا بلا أي أثر.

سكيما `auth` مملوكة لـ`supabase_admin`، و`postgres` يحمل `USAGE` عليها **بلا
`GRANT OPTION`**. فمحاولة تمريرها لدور آخر تُصدر:

```
WARNING:  no privileges were granted for "auth"
GRANT
```

**تحذيرًا لا خطأً** — فيمر الترحيل بحالة نجاح ويبقى الدور بلا صلاحية. اكتشفناه
فقط عند أول تنفيذ فعلي للدالة، لا عند الترحيل.

**الالتفاف:** لا تعتمد على سكيما `auth` داخل دوال يملكها دور مخصص. استخدم
`private.current_uid()` التي تقرأ الـGUC مباشرة (الترحيل 0018).

**درس عام:** `GRANT` قد يُصدر تحذيرًا بدل خطأ. لا تعتبر «الترحيل نجح» دليلًا
على أن الصلاحية مُنحت — تحقق بـ`has_schema_privilege`.

---

## BD-ENV-003 — PostgREST يضبط `request.jwt.claims` فقط

**الخطورة:** عالية لو بُني عليها خطأً.

مُثبَت بـJWT حقيقي عبر Kong → PostgREST على هذا الستاك:

| GUC | القيمة |
|---|---|
| `request.jwt.claim.sub` (مفرد) | **NULL** |
| `request.jwt.claims` (JSON) | الحمولة كاملة مع `sub` |

فأي دالة هوية تقرأ المفتاح المفرد وحده تُرجع `NULL` لكل طلب حقيقي، وتُفشل كل
RPC بـ`BD403`. الرجوع إلى الـJSON **ليس احتياطًا تجميليًا** بل المسار العامل.

`private.current_uid()` تقرأ الاثنين بنفس ترتيب `auth.uid()` الأصلية.

---

## ملاحظات على قيود PostgreSQL (سلوك موثَّق، لا أعطال)

| السلوك | الأثر علينا |
|---|---|
| `SELECT … FOR UPDATE` تتطلب `UPDATE` لا `SELECT` | مُنح `update (retired_at)` على `fabric_rolls` — صلاحية العمود الواحد تكفي (مُتحقَّق منه) |
| `ALTER … OWNER TO` تتطلب `CREATE` على السكيما | تُمنح مؤقتًا ثم تُسحب في نهاية كل ترحيل |
| `ON DELETE SET NULL` على مفتاح مركّب | تحتاج قائمة أعمدة صريحة (PG 15+) وإلا صُفِّر `organization_id` |
| view بـ`security_invoker` | لا يحتاج مستدعيه `USAGE` على سكيما الجدول الأساسي (مُتحقَّق منه) |
